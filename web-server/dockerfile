# 1️⃣ Use the official Node.js base image
FROM node:18-alpine

# 2️⃣ Set working directory inside the container
WORKDIR /app

# 3️⃣ Copy package.json and package-lock.json first
COPY package*.json ./

# 4️⃣ Install dependencies
RUN npm install --production

# 5️⃣ Copy all other files into the container
COPY . .

# 6️⃣ Expose your app’s port (adjust if needed)
EXPOSE 8085

# 7️⃣ Start the app
CMD ["node", "app.js"]
